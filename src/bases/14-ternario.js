
let firstName;
let lastName = 'Martinez';

// console.log(`${firstName || 'No firstName'} ${lastName || 'No firstName'}`);

const isActive = true;

const msg = (isActive)  ? 'Activo' : 'Inactivo'

// if(isActive) {
//   msg = 'Activo'
// } else {
//   msg = 'Inactivo'
// }

console.log(msg);